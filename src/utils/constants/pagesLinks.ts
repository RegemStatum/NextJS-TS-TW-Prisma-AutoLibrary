import { HomeIcon, BookIcon, AuthorIcon } from "@/components/ui/icons";

const pagesLinks = [
  { id: 1, name: "Home", path: "/", LinkIcon: HomeIcon },
  { id: 2, name: "Books", path: "/books", LinkIcon: BookIcon },
  { id: 3, name: "Authors", path: "/authors", LinkIcon: AuthorIcon },
];

export default pagesLinks;
