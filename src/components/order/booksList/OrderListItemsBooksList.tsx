import { OrderInfoBook, OrderInfoCabinet } from "@/types/OrderInfo";
import React, { FC } from "react";

interface Props {
  books: OrderInfoBook[];
}

const OrderListItemsBooksList: FC<Props> = ({ books }) => {
  return <div>OrderListItemsBooksList</div>;
};

export default OrderListItemsBooksList;
